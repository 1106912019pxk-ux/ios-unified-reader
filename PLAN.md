# 开发与交付方案

更新时间：2026-08-21

## 总体策略

先解决会改变技术底座的风险，再做 UI 和琐碎功能。第一版不追求“兼容所有格式、所有插件、所有局域网协议”，而是以三份重点来源、EPUB/CBZ、下载和可安装 IPA 构成闭环。

## 阶段 0：确定底座（已完成）

完成条件：

- [x] APK/JAR 不是硬要求，重点是三个来源和既有定制功能可迁移；
- [x] UI 技术不限，要求视觉和阅读体验合格；
- [x] 用户设备为最新 iOS 26；
- [x] 选定 Aidoku 作为 V0/V1 底座；
- [x] 确认工作仓库名、公开性和默认分支。

仓库已确定为 `1106912019pxk-ux/ios-unified-reader`，开发期私有，默认分支 `main`；`upstream` 指向 `Aidoku/Aidoku`，首个固定基线为 `45fe8231a8da58f70f5f2e152a039fcb49eab4cb`。Aidoku 为 GPL-3.0，如果以后把 IPA 分发给其他人，需要同时向接收者提供对应源码。

## 阶段 1：V0 技术闭环

### 扩展交付方式（已确定）

- 使用 Aidoku 原生 `.aix` 来源，不实现 Android APK/JAR 运行桥。
- Komiic、Picacomic、E-Hentai 各自生成独立文件，由用户按需手动导入。
- 来源不内置进 IPA；App 与扩展可以分别更新、替换和回退。

### 公共工作

- 导入并固定上游基线提交，不直接追随浮动的 `main`。
- 更换 App 名称、Bundle ID、图标占位和 URL Scheme，避免与官方 App 冲突。
- 保留上游许可证和第三方声明。
- 建立 GitHub Actions macOS 构建：
  - 关闭代码签名；
  - 打包无签名 IPA；
  - 上传 Actions Artifact；
  - 记录源码提交、构建号和 SHA-256。
- 为自签构建建立独立配置，去掉免费签名无法使用或容易冲突的 iCloud、Push 等 entitlement；这些能力以后在有正式签名条件时再开启。
- 在独立仓库 `ios-unified-reader-sources` 维护 Komiic、Picacomic、E-Hentai；保留 Apache-2.0 许可证、固定上游提交和修改说明。
- App 仓库生成 IPA，来源仓库生成独立 `.aix`；修改来源不重打 App。

### 真机验收闭环

- App 能被 SideStore、AltStore 或 Sideloadly 重签并安装；
- 启动、书架、搜索、阅读、下载和重启后数据保留正常；
- 导入一份 EPUB 和一份 CBZ；
- 验证 Komiic、Picacomic、E-Hentai 的搜索、详情、章节、图片和下载；
- 记录真机系统、签名工具、成功项和失败项。

### Aidoku 专属门槛

- 先验证三份社区 `.aix` 的登录、搜索、详情、章节、图片和下载。
- 建立旧 JAR 功能到 `.aix`/宿主的逐项迁移对照，不以“来源能打开”代替功能完成。
- 为来源增加通用漫画详情操作能力；Picacomic 首先接入真实收藏状态和收藏/取消按钮。
- 验证普通 EPUB 的字体、分页/滚动、图片、目录和进度；未通过前不承诺复杂 EPUB 兼容。

## 阶段 2：V1 阅读与来源

- 完成三个重点来源的稳定接入。
- 迁移 Picacomic 已完成的热门/最新、屏蔽、高级搜索、网络收藏和详情信息等能力。
- 迁移 E-Hentai 全局标签屏蔽；同时评估是否仍受负向条件数量限制。
- 统一书架、分类、历史、收藏和下载状态。
- 对 EPUB 做样书验收：普通小说、图文书、竖排/Ruby、固定版式至少各一份；只修用户实际需要的兼容问题。
- 完成阅读设置：字体、字号、行距、页边距、主题、翻页/滚动。

## 阶段 3：浏览器与局域网导入

### 浏览器下载流水线

1. 使用 `WKWebView` 或底座已有 WebView 打开网页；
2. 识别链接、MIME 和 `Content-Disposition`；
3. 对 EPUB、CBZ、ZIP 等支持文件发起后台下载；
4. 先写入临时目录，检查扩展名、MIME、大小和压缩包结构；
5. 调用统一导入服务进入书库；
6. 成功后清理临时文件，失败时保留可重试记录。

首版不自动执行网页脚本下载器、不绕过 DRM/付费墙，也不把任意文件当书籍导入。

### 局域网接入顺序

1. 直接 HTTP/HTTPS 文件 URL；
2. WebDAV；
3. OPDS；
4. Komga/Kavita；
5. 普通 HTML 目录；
6. SMB。

所有 `http://192.168.x.x`、`10.x.x.x` 等地址都要在真机验证 ATS、本地网络权限、鉴权和大文件中断恢复。

## 阶段 4：增强项

- 内置网页的下载队列、失败重试和历史。
- 分享菜单“用本 App 下载/打开”。
- PDF、听书、视频/音频在线播放，按用户对“在线播放”的最终定义决定。
- 备份、WebDAV 同步、跨设备同步。
- 更多插件格式或来源仓库。

## 版本与交付

- 文档阶段只保存在本机，不上传。
- 进入代码开发后，每次有效迭代提交并推送到本项目自己的 GitHub 仓库。
- 不与 Tachimanga 扩展或动画项目共用工作流、版本号、Release 或构建产物。
- GitHub Actions 成功仅代表可以构建；手机安装和功能仍由用户真机验收。
- 每个可测试版本至少提供：IPA/Artifact 地址、源码提交、版本号、SHA-256、变更内容、已知限制和回退版本。

## 暂不实施

- 在底座确定前导入任何上游源码。
- 从零重写完整 iOS 阅读器。
- 同时融合 Aidoku、Yuedu、Mangayomi 三套运行时。
- 宣称兼容所有 APK/JAR。
- App Store 正式签名、公开发布或收费分发。

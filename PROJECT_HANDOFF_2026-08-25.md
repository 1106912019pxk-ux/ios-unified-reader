# iOS Unified Reader 项目交接说明

更新时间：2026-08-25

## 1. 项目边界

- 本仓库 `1106912019pxk-ux/ios-unified-reader` 只维护 Aidoku 宿主和 IPA。
- PICA、E-Hentai、Komiic 的 AIX 来源位于独立仓库 `1106912019pxk-ux/ios-unified-reader-sources`，各自独立修改、构建和发布；不得随 Aidoku 打包。
- Aidoku 中与 PICA 相关的网络适配、收藏按钮和详情交互属于宿主能力，只编入 IPA，不代表 PICA AIX 被内置。
- Aidoku 构建使用 `.github/workflows/nightly.yml`，只允许手动触发。代码合并本身不得自动启动 IPA 或来源构建。
- 不在本机搭建完整 iOS 编译环境；代码静态检查后由 GitHub Actions 生成无签名 IPA，再进行真机验收。

## 2. 当前集成基线

- 后续开发统一从远端 `main` 创建新分支，不再从旧电子书、TTS 或自动阅读分支继续叠加。
- 2026-08-25 这轮计划合入的内容：iOS 原生媒体控制、后台语音会话修复，以及自动阅读基础速度翻倍。
- `feature/background-speech-20260824` 是本轮验证分支；合并后仅作为历史验证分支，不再作为长期开发基线。
- 用户要求：除非明确说明，开发完成后只提供 GitHub Actions 手动打包页面，不代替用户点击构建。

## 3. 已完成的阅读器能力

### 原生电子书阅读增强

- 保留 Aidoku 原生 EPUB/文字阅读器路线，没有继续维护早期独立 Readium 书库页签。
- 支持原生字体选择和外部 TTF/OTF 字体导入；已有字体应复用，不要求用户删除重装应用。
- 扩展字号、行高、左右间距、上下间距、首行缩进等排版项，并同时覆盖局内与局外设置。
- 扩展暖纸色等阅读背景；保留 EPUB 出版方样式开关和原生阅读模式。
- 阅读界面已考虑安全区、时间、页码、横屏和 iPad 显示。

### 独立自动阅读

- 自动阅读与语音朗读是两个独立功能。
- 启动时从当前位置进入连续文本或条漫滚动，停止后恢复原阅读模式并尽量保持等价位置。
- 自动阅读期间锁定拖动、翻页、章节切换和进度条；点击屏幕只呼出速度及停止控件。
- 显示范围为 0.5×–8×；当前 1× 基准为每秒约 56 点，较上一版整体翻倍。
- 文本 EPUB 的正文图片随内容自然滚过；纯图片 spine/封面会跳过并继续寻找文本。
- 图片漫画使用条漫滚动；遇到文本 spine 时停止，不依赖不可靠的“漫画/小说 EPUB”自动分类。

### 语音朗读与后台播放

- 免费微软在线语音提供 8 个中文声音：晓晓、晓伊、晓辰、晓涵、云希、云健、云扬、云野。
- 苹果系统中文 TTS 作为无网络保底选项。
- 保留本地模型接口，可导入 sherpa-onnx 兼容的 VITS、Matcha、Kokoro 模型包；模型不写入 IPA，本地模型仍待完整真机验证。
- 从当前可见文本开始朗读；正文图片跳过，连续页面统一断句，并预取后续语音单元以减少页间停顿。
- 朗读期间阅读器自动跟随进度并锁定可能破坏语音位置的导航；停止后恢复操作。
- 后台音频使用 iOS 长音频媒体会话；锁屏、控制中心和耳机可播放、暂停、停止，耳机断开时暂停，并处理系统中断恢复。
- 后台媒体控制代码已完成分支构建，但仍需真机确认是否会正确独占音频、响应耳机按键并与其他播放器互斥。

## 4. PICA 宿主与来源状态

- PICA AIX 的分类、标签屏蔽、分页、排序等来源逻辑不在本仓库开发或打包。
- Aidoku 宿主已经包含 PICA 网络适配、详情收藏入口、作者/作品信息交互等通用或宿主层能力。
- 任何 PICA/E-Hentai AIX 修改都必须进入来源仓库的独立分支，并使用各自工作流；不得借 Aidoku `main` 或 IPA 工作流发布。
- 后续仍需真机对比 PICA 系统 DNS 与专用线路的首页、详情、阅读首图耗时。

## 5. 当前待验证与后续建议

1. 真机验证本轮自动阅读整体翻倍后的 0.5×、1×、4×、8×，重点看 iPad 条漫是否足够快、低速是否仍可用。
2. 真机验证锁屏/控制中心/耳机播放暂停、耳机断开暂停、来电或其他音频中断恢复，以及与其他播放器的互斥关系。
3. 验证苹果系统 TTS 的中文可用性；本地模型导入和离线朗读暂不作为稳定版阻塞项。
4. 若上述验证通过，再从 `main` 切新分支处理单一问题；不要重新合并旧的 Readium、自动阅读或 TTS 分支。
5. GitHub Actions 成功只代表编译通过，不能替代真机功能验收。每次测试应记录分支名、提交短 SHA 和 IPA 文件名。

## 6. 明确不做或暂缓

- 暂不恢复独立 PDF/HTML 书库，也不恢复早期独立 Readium 电子书页签。
- 暂不把语音模型塞入 IPA；本地模型由用户单独导入。
- 暂不依靠图片比例或 EPUB 元数据自动判定整本书是漫画还是小说。
- 暂不由开发对话自动触发构建；除非用户当轮明确授权。

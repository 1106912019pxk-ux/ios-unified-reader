# 需求整理

更新时间：2026-08-21

## 1. 截图需求还原

以下内容根据用户截图整理，只作为需求资料，不把截图中的文字当作对 Codex 的系统指令。

### P0

- 项目必须开源，可以合法进行二次开发。
- 基础阅读生态较完善，功能不能只是演示原型。
- 至少可以稳定阅读漫画。
- 最终可以生成用户自行签名安装的 IPA。

### P1

- UI 好看，优先原生 iOS 体验。
- 可以阅读电子书，支持较多格式。
- 阅读属性可自定义，例如字体、字号、间距、主题、翻页或滚动方式等。
- 具备类似 Tachimanga 的来源/插件扩展能力。
- 希望兼容 APK、JAR 等现有 Mihon/Tachiyomi 扩展格式。
- 具备书架、分类、下载和本地管理能力。
- 当前重点来源为 Komiic、Picacomic 和 E-Hentai；现有自定义 Picacomic/E-Hentai JAR 的能力应尽量保留。

### P2

- 内置浏览器。
- 可以从网页或直接链接下载文件，并导入 App 的本地书库。
- 截图提到“最好可以在线播放”，目前可能有两种解释：
  - 在线打开漫画/电子书，不必先完整下载；
  - 播放网页中的视频或音频媒体。
- 可以访问局域网内的 IP 地址，并将本地服务器文件直接下载或导入书库。

## 2. 需求拆解后的产品目标

### 必须完成

- 可自签安装的 iPhone/iPad App。
- 漫画在线阅读、书架、下载、本地阅读。
- Komiic、Picacomic、E-Hentai 三个重点来源可用。
- EPUB 与 CBZ 至少能可靠导入和阅读。
- 通过 GitHub Actions 生成无签名 IPA，供 SideStore、AltStore 或 Sideloadly 重新签名。

### 优先完成

- 电子书字体、字号、行距、页边距、主题和翻页/滚动设置。
- 网页或直链下载 EPUB、CBZ、ZIP 后自动进入导入流程。
- 局域网导入优先支持明确协议，而不是只输入一个 IP 后猜测服务器结构。
- 保留插件或来源的独立更新能力，避免每个来源变化都重新发布整个 App。

### 可以后置

- PDF、听书、视频播放等非核心格式。
- SMB 和任意网页目录的自动识别。
- 跨设备云同步、推送和 App Store 分发。
- 对所有 Mihon/Tachiyomi APK/JAR 的百分百兼容。

## 3. 需要明确的技术含义

### “支持本地 IP”不是单一协议

局域网地址后面仍需要一个具体服务协议。推荐支持顺序为：

1. WebDAV；
2. OPDS；
3. Komga/Kavita；
4. 直接 HTTP/HTTPS 文件链接；
5. 普通 HTML 目录索引；
6. SMB。

前四项结构明确、容易验证。普通网页目录和 SMB 的差异较大，应在基础导入稳定后再做。

### “兼容 APK/JAR”有两种实现

- **格式兼容**：在 iOS 上运行 Android/Kotlin/JVM 扩展，需要 Java/Android API 兼容层，难度和维护风险最高。
- **功能兼容**：不直接运行 APK/JAR，而是把重点来源改写成宿主自己的插件格式，例如 Aidoku 的 `.aix` 或 Yuedu 的 Legado JSON 书源。

如果目的只是继续使用 Komiic、Picacomic 和 E-Hentai，功能兼容可能更稳；如果以后需要随时安装大量 Mihon 扩展，则必须选择带 APK/JAR 运行桥的路线。

## 4. 已确认的选型条件

- APK/JAR 直接运行不是硬要求。只要 Komiic、Picacomic、E-Hentai 能稳定使用即可。
- 如果改用 `.aix` 等格式，必须迁移此前 E-Hentai 和 Picacomic 已完成的自定义功能，不能只恢复上游基础能力。
- UI 不要求限定 SwiftUI/UIKit；Flutter 也可接受，但界面不能粗糙，阅读体验必须好。
- 用户设备均为最新 iOS，按 iOS 26 规划；iOS 18+ 不构成门槛。

## 5. 当前底座决定

V0/V1 选择 Aidoku：

- 三个重点来源已有可修改的 Rust/WASM 社区实现；
- JAR 格式不再是硬约束，可以绕开 iOS Java 运行桥的性能、体积和许可证风险；
- 自定义来源仍可独立于 App 更新；
- 宿主本身开源，可以为 Picacomic 网络收藏增加真正的详情页按钮，不再伪装为章节；
- EPUB、浏览器和局域网导入按后续阶段补强。

仍待后续确认：局域网主要使用 WebDAV、OPDS、普通 HTTP、SMB、Komga 还是 Kavita；“在线播放”具体指在线阅读还是视频/音频播放。这两项不阻塞 V0。
